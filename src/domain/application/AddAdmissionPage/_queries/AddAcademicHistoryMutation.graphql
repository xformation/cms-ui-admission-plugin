mutation addAcademicHistory($input: AddAcademicHistoryInput!) {
  addAcademicHistory(input: $input) {
    academicHistory {
      id
      qualification
      yearOfPassing
      institution
      university
      enrollmentNo
      score
      percentage
    }
  }
}